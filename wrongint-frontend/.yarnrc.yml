nodeLinker: node-modules

# Supply-chain hardening: refuse package versions published less than 14 days
# ago, so freshly-compromised releases can't slip in (DURATION, base unit minutes).
npmMinimalAgeGate: 14d
